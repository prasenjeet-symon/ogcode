title: ogcode
description: Every coding agent re-sends your entire conversation, every turn. Ogcode recalls only what matters — 70% fewer tokens, no context ceiling, sharper answers. Self-hosted, one Go binary, bring your own model.
url: "https://ogcode.xyz"

theme: null

# Serve everything from docs/ as static files
# install.sh will be served as text/plain automatically by GitHub Pages
include:
  - install.sh